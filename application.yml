logging:
  config: classpath:log/logback.xml
spring:
  devtools:
    restart:
      enabled: false
server:
  tomcat:
    uri-encoding: UTF-8
eureka:
  client:
    serviceUrl:
       defaultZone: http://localhost:8887/discovery/eureka
  instance:
    healthCheckUrlPath: ${server.context-path}/health
    statusPageUrlPath: ${server.context-path}/info    